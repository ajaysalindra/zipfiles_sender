<div class="form-horizontal">
<%= form_for @cyborg do |f| %>
		<p>
			<%= f.label :name, "Name :" %>
			<%= f.text_field :name %>
		</p>

		<p>
			<%= f.label :email, "Email :" %>
			<%= f.text_field :email %>
		</p>

		<p>
			<%= f.submit "Create" %>
		</p>
<% end %>
</div>